# Integration Tests

Cross-package tests live here when no single package owns the full behavior.
Fixtures must be small, redistributable, de-identified, and documented with
their expected result and provenance.

