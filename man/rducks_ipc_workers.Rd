% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provider_nng.R
\name{rducks_ipc_workers}
\alias{rducks_ipc_workers}
\title{List Rducks-managed IPC workers}
\usage{
rducks_ipc_workers(con = NULL, ping = FALSE, timeout = 1)
}
\arguments{
\item{con}{Optional DuckDB connection. When supplied, only providers attached
to that connection's Rducks runtime token are listed. With \code{NULL}, all
providers known to this R process are listed.}

\item{ping}{Logical scalar. If \code{TRUE}, send a lightweight NNG ping to every
listed endpoint and report \code{ok} or the ping error.}

\item{timeout}{Positive timeout in seconds used for \code{ping = TRUE}.}
}
\value{
A data frame with one row per configured worker endpoint.
}
\description{
Lists the local Rducks NNG providers currently known to this R process. These
are the managed workers used by \code{transport = "ipc"} scalar-UDF
execution plans when \code{ipc_endpoints} is not supplied. Caller-supplied external
endpoints are shown as external providers.
}
\examples{
\donttest{
db <- duckdb::dbConnect(duckdb::duckdb(config = list(allow_unsigned_extensions = "true")))
rducks_enable(db)
rducks_ipc_workers(db)
rducks_release(db)
DBI::dbDisconnect(db)
}
}
