% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execution_plan.R
\name{rducks_execution_plan}
\alias{rducks_execution_plan}
\title{Define an Rducks execution plan}
\usage{
rducks_execution_plan(
  transport = "inproc",
  ipc_globals = "auto",
  ipc_packages = NULL,
  ipc_timeout = NULL,
  ipc_endpoints = NULL,
  ipc_transport = NULL,
  ipc_globals_share = "none",
  ipc_provider = "nng",
  ipc_workers = 1L,
  ipc_max_pending = 64L
)
}
\arguments{
\item{transport}{Placement/transport. \code{"inproc"} evaluates in the current R
process with the in-process queued backend. \code{"ipc"} evaluates in persistent
worker R processes over NNG; when \code{ipc_endpoints} is \code{NULL}, Rducks starts
local worker loops with mirai daemons.}

\item{ipc_globals, ipc_packages, ipc_timeout, ipc_endpoints, ipc_transport}{IPC worker options (used when \code{transport = "ipc"}).
By default (\code{ipc_globals = "auto"}), Rducks discovers scalar-UDF globals
once at registration-wrapper creation and broadcasts them to each NNG worker
when the scalar UDF is registered with the shared provider pool. Automatic capture
estimates the serialized globals payload and warns when it exceeds option
\code{rducks.ipc_globals.warn_bytes} (8 MiB by default); option
\code{rducks.ipc_globals.max_bytes} can set a hard byte limit. Set
\code{ipc_globals_share = "mori"} to pass selected globals through mori shared
memory references for same-host workers; Rducks keeps the shared objects
anchored for the registered scalar UDF lifetime. Use \code{ipc_packages} for packages
that workers should attach, \code{ipc_globals = FALSE} to rely only on the
serialized UDF closure and explicit task state, or a character vector /
named list for explicit extra globals. \code{ipc_timeout} is the positive finite
provider wait timeout in seconds; \code{NULL} uses a finite default of 30 seconds.
\code{ipc_endpoints} optionally supplies NNG endpoint URLs for worker processes
that the caller starts and stops; those processes must run the Rducks NNG
worker loop. Any NNG URL transport supported by both endpoints is allowed.
When endpoints are not
supplied, \code{ipc_transport} selects the transport used for the mirai-launched
local worker endpoints and must be left as \code{NULL} when explicit
\code{ipc_endpoints} are supplied. Rducks retries local TCP/WebSocket startup
with fresh endpoint bundles after startup-ping failure; caller-supplied
endpoints remain caller-owned and fail fast. \code{"abstract"} means Linux abstract IPC, \code{"ipc"}
means NNG IPC (Unix-domain sockets on POSIX and named pipes on Windows),
\code{"unix"} means the POSIX Unix-domain alias, and \code{"tcp"} / \code{"ws"} use
loopback TCP / WebSocket endpoints. The default is \code{"abstract"} on Linux
and \code{"ipc"} elsewhere.}

\item{ipc_globals_share}{How selected IPC globals are represented before
worker broadcast. \code{"none"} serializes them into the registration payload.
\code{"mori"} applies \code{mori::share()} to each selected global before
serialization, which can turn large atomic vectors, lists, and data frames
into same-host shared-memory references. This requires the optional mori
package and workers on the same machine.}

\item{ipc_provider}{Worker provider for \code{transport = "ipc"}.
Only \code{"nng"} is supported. The NNG provider broadcasts each registered scalar UDF
closure plus discovered globals/packages to every worker in the shared
database-runtime provider pool, so avoid capturing large objects in UDF
environments unless that memory cost is intended or \code{ipc_globals_share = "mori"} is appropriate.}

\item{ipc_workers}{Number of persistent NNG workers.}

\item{ipc_max_pending}{Maximum simultaneous native NNG requests admitted per
registered scalar-UDF client pool. \code{NULL} uses the provider default of 64.
Non-IPC plans store \code{NA_integer_} for this field. The current provider still
uses synchronous request/reply per callback rather than collect-many
batching, but this value is enforced as a bounded pending/in-flight guard
before a callback enters the native request path.}
}
\value{
An object of class \code{rducks_execution_plan}.
}
\description{
An execution plan describes where Rducks evaluates registered scalar-UDF
chunks: in the current R process (\code{transport = "inproc"}) or in persistent
worker R processes (\code{transport = "ipc"}). When stored on a connection it is
the default for future
\code{\link[=rducks_register_scalar_udf]{rducks_register_scalar_udf()}}
calls and updates the native runtime backend used for matching concurrent
execution; the resolved transport metadata is frozen into each registered
scalar UDF's database-catalog metadata. It is separate from DuckDB function
kind and from scalar-UDF registration semantics such as Rducks evaluation
mode (\code{"scalar"} row calls versus \code{"vectorized"} chunk calls),
argument/return types, NULL handling, error handling, and side effects.
}
\details{
\code{"inproc"} keeps all R API work serialized on the recorded main R thread
while allowing DuckDB callback concurrency; DuckDB vectors are materialized
to SEXPs directly in extension C with no intermediate columnar format. It
maps to the internal \code{"direct_main_queue"} engine.

\code{"ipc"} uses persistent NNG/nanonext worker R processes that exchange
Quack-style binary chunk payloads (a DuckDB BinarySerializer subset): the
extension encodes each input chunk to wire bytes, the worker decodes them,
runs the R function, and returns wire-encoded results that the extension
writes back to DuckDB. Worker-process types currently cover fixed-width
scalars, \code{VARCHAR}/\code{BLOB}, \code{DECIMAL}, \code{INTERVAL}, \code{ENUM}, \code{BIT}, \code{GEOMETRY}, \code{MAP}, \code{UNION}, and
\code{LIST}/\code{ARRAY}/\code{STRUCT} of supported types; \code{VARIANT} is rejected at registration until the
native bridge covers it. It maps to the internal \code{"ipc_nng_pool"} engine.
}
\examples{
rducks_execution_plan("inproc")
}
