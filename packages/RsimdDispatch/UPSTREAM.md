# Upstream Record

This package was imported from `sounkou-bioinfo/RsimdDispatch` at the commit
recorded in `UPSTREAM.dcf`. It supplies the current SIMD headers, configure
templates, and guarded runtime-dispatch interfaces required by packages such as
`Rminibwa`.

Updates should be imported as reviewed subtree changes. Preserve the package's
GPL-2.0-or-later terms and the bundled SIMDe MIT license, authorship, and
notices.
