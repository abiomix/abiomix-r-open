# Upstream Record

This package was imported from `sounkou-bioinfo/Rtinycc` at the commit recorded
in `UPSTREAM.dcf`. It provides the packaged TinyCC CLI, libtcc runtime, and FFI
interfaces used by native R package tests and runtime compilation workflows.

Updates should be imported as reviewed subtree changes. Preserve the package's
GPL-3.0-or-later terms, TinyCC's LGPL-2.1 terms, and all adapted-component
notices recorded in `inst/LICENSE.note`.
