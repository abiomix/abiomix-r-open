# Upstream Record

This package was imported from `rstudio/httpuv` at the commit recorded in
`UPSTREAM.dcf`. The Abiomix fork exists to implement and validate
standards-compliant HTTP byte-range serving for large immutable artifacts.

Keep the patch narrowly focused and submit generally useful behavior upstream.
Preserve Posit, libuv, http-parser, and all other bundled copyright and license
notices when synchronizing upstream changes.
