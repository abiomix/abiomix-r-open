# Upstream Record

This package was imported from `hyperverse-r/htmxr` at the commit recorded in
`UPSTREAM.dcf`. The Abiomix fork provides a controlled public integration point
for the product UI stack while remaining suitable for upstream contributions.

Updates should be imported as reviewed subtree changes. The original code's MIT
license and attribution must remain intact. Abiomix fork changes are publicly
distributed under GPL-2.0-or-later.
