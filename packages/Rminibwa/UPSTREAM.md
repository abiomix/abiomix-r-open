# Upstream Record

This package was imported from `sounkou-bioinfo/Rminibwa` at the commit recorded
in `UPSTREAM.dcf`. The Abiomix fork is the maintained public R/native alignment
package used to standardize the short-read stack.

Updates should be imported as reviewed subtree changes. Preserve the attribution
and license terms of `minibwa`, `libsais`, QSufSort, BWT-SW, and other bundled
components together with the package's GPL-2.0-or-later terms.
