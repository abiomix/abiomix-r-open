/*
 * RBCFTools builds its native command-line tools from nested upstream source
 * trees during configure. The package intentionally exposes no R shared
 * library; src/Makevars therefore leaves this translation unit uncompiled.
 */
