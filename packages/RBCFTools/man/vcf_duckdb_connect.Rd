% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcf_duckdb.R
\name{vcf_duckdb_connect}
\alias{vcf_duckdb_connect}
\title{Setup DuckDB connection with bcf_reader extension loaded}
\usage{
vcf_duckdb_connect(
  extension_path,
  dbdir = ":memory:",
  read_only = FALSE,
  config = list()
)
}
\arguments{
\item{extension_path}{Path to the bcf_reader.duckdb_extension file.
Must be explicitly provided.}

\item{dbdir}{Database directory. Default ":memory:" for in-memory database.}

\item{read_only}{Logical, whether to open in read-only mode. Default FALSE.}

\item{config}{Named list of DuckDB configuration options.}
}
\value{
A DuckDB connection object with bcf_reader extension loaded
}
\description{
Creates a DuckDB connection and loads the bcf_reader extension for VCF/BCF queries.
}
\examples{
\dontrun{
# First build the extension
ext_path <- bcf_reader_build(tempdir())

# Then connect
con <- vcf_duckdb_connect(ext_path)
DBI::dbGetQuery(con, "SELECT * FROM bcf_read('variants.vcf.gz') LIMIT 10")
DBI::dbDisconnect(con)
}
}
