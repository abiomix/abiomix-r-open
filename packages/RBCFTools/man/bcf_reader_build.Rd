% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcf_duckdb.R
\name{bcf_reader_build}
\alias{bcf_reader_build}
\title{Build the bcf_reader DuckDB extension}
\usage{
bcf_reader_build(build_dir, force = FALSE, verbose = TRUE)
}
\arguments{
\item{build_dir}{Directory where to build the extension. Source files will
be copied here and the extension will be built in \verb{build_dir/build/}.}

\item{force}{Logical, force rebuild even if extension exists}

\item{verbose}{Logical, show build output}
}
\value{
Path to the built extension file
}
\description{
Compiles the bcf_reader extension from source using the package's htslib.
Source files are copied to the build directory first.
}
\examples{
\dontrun{
# Build in temp directory
ext_path <- bcf_reader_build(tempdir())

# Build in a specific location
ext_path <- bcf_reader_build("/tmp/bcf_reader")

# Force rebuild
ext_path <- bcf_reader_build("/tmp/bcf_reader", force = TRUE)
}
}
