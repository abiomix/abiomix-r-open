% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain.R
\name{rducks_reset_udf_counters}
\alias{rducks_reset_udf_counters}
\title{Reset Rducks scalar-UDF counters}
\usage{
rducks_reset_udf_counters(con, name = NULL)
}
\arguments{
\item{con}{A \code{duckdb_connection} with Rducks enabled.}

\item{name}{Optional SQL scalar-UDF function name registered with
\code{\link[=rducks_register_scalar_udf]{rducks_register_scalar_udf()}}. If
\code{NULL}, reset counters for all native Rducks scalar UDFs in the database runtime.}
}
\value{
Invisibly \code{TRUE} on success.
}
\description{
Resets native per-scalar-UDF diagnostic counters without unregistering any
DuckDB catalog function. Current liveness gauges such as pending/in-flight
counts are preserved; their max fields are reset to the current values.
}
\examples{
\donttest{
db <- duckdb::dbConnect(duckdb::duckdb(config = list(allow_unsigned_extensions = "true")))
rducks_enable(db, threads = "single")
rducks_register_scalar_udf(db, "my_fn", function(x) x + 1L,
  args = list(INTEGER), returns = INTEGER)
rducks_reset_udf_counters(db, "my_fn")
rducks_release(db)
DBI::dbDisconnect(db)
}
}
