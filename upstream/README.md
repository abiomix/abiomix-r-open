# Upstream Records

Repository-level upstream synchronization recipes and shared provenance records
live here. Package-specific source identity and patch notes remain inside each
package as `UPSTREAM.dcf` and `UPSTREAM.md`.

